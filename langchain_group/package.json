{
  "name": "langchain_group",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "activate": "source .venv/bin/activate",
    "first": "python3 -m venv .venv",
    "freeze": "pip freeze > requirements.txt",
    "install": "pip install -r requirements.txt",
    "start": "rm -rf .cache; python app.py"
  }
}
